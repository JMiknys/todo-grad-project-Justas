general:
  artifacts:
    - "build_artifacts"
deployment:
  prod:
    branch: master
    heroku:
      appname: todo-grad-project-justas
machine:
  node:
    version: 6.3.1
dependencies:
  pre:
    - npm install grunt-cli
